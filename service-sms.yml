server:
  port: 8002
meta-map:
  env: service-sms-append-2020年08月05日:8002
management:
  endpoint:
    health:
      show-details: always
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"